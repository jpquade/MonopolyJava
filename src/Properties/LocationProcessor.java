package Properties;

import Misc.Player;

public class LocationProcessor {

    public void locationProcessing(Player player, Integer boardLocation){
        // empty - come up way of dealing with rental on property or buying it

        player.setBoardLocation(boardLocation);
    }

    public void locationProcessing(Player player, Integer boardLocation, Integer paymentModification){
        // empty - come up way of dealing with rental on property or buying it

        player.setBoardLocation(boardLocation);
    }
}
